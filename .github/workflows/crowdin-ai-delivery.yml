name: crowdin-ai-delivery

on:
  push:
    branches:
      - aidan/crowdin-ai-recording-02

permissions:
  contents: read
  pull-requests: read

jobs:
  pending:
    if: github.repository == 'aidanaden/jupiter-i18n-translation-pilot' && github.ref == 'refs/heads/aidan/crowdin-ai-recording-02'
    permissions:
      contents: read
      pull-requests: read
      statuses: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/provider-e2e/crowdin-ai-delivery-status.mjs pending
        env:
          GH_TOKEN: ${{ github.token }}

  crowdin-ai-delivery:
    needs: pending
    if: github.repository == 'aidanaden/jupiter-i18n-translation-pilot' && github.ref == 'refs/heads/aidan/crowdin-ai-recording-02'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - name: Read current isolated Crowdin records
        env:
          CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_AI_RECORDING_TOKEN }}
        run: node scripts/provider-e2e/crowdin-ai-native-read.mjs "$RUNNER_TEMP/crowdin-ai-delivery-native"
      - name: Verify exact PR catalog and native approvals
        env:
          GH_TOKEN: ${{ github.token }}
        run: node scripts/provider-e2e/crowdin-ai-delivery.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: crowdin-ai-delivery-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/crowdin-ai-delivery-receipt.json
          retention-days: 7
          if-no-files-found: error

  publish-status:
    if: always() && github.repository == 'aidanaden/jupiter-i18n-translation-pilot' && github.ref == 'refs/heads/aidan/crowdin-ai-recording-02'
    needs: [pending, crowdin-ai-delivery]
    permissions:
      contents: read
      pull-requests: read
      statuses: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/provider-e2e/crowdin-ai-delivery-status.mjs complete
        env:
          GH_TOKEN: ${{ github.token }}
          VERIFY_RESULT: ${{ needs.crowdin-ai-delivery.result }}
          PENDING_RESULT: ${{ needs.pending.result }}
